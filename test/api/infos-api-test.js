import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { venuelyService } from "./venuely-service.js";
import { maggie, mozart, testvenues, testinfos, concerto } from "../fixtures.js";

suite("info API tests", () => {
  let user = null;
  let beethovenSonatas = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("create info", async () => {
  });

  test("create Multiple infoApi", async () => {
  });

  test("Delete info", async () => {
  });

  test("test denormalised venue", async () => {
  });
});
