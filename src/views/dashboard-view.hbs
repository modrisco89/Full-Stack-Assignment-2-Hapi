{{> menu active="dashboard"}}

<section class="section">
  {{#if user.admin}}
  {{> list-analytics}}
  {{/if}}
  {{#unless user.admin}}
  {{> list-venues}}
  {{> add-venue}}
  {{> show-map}}
  {{/unless}}

</section>
